<!DOCTYPE html>
<html>
  <head>
    <% title = [ content_for( :title ), "Maitre D" ].compact.join(" | ") %>
    <title><%= title %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" media="all" href="/pico/pico-v1.3.3.min-dist.css">
    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track": "reload" %>
  </head>

  <body>
    <main class="container"><%= yield %></main>
    <footer>
      <div class="container">
        <%= link_to "Logout", logout_path %>
      </div>
    </footer>
  </body>
</html>
